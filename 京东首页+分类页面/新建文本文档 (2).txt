外面包裹一个大盒子-----实现全屏页面----html，body，和大盒子--width100%，height：100%

头部：左右两个a标签，中间一个form表单
头部大盒子：宽高，定位，背景色，边框等
左右两个a链接：设置宽高，背景图，--使用background:position调整位置
/*设置是裁切，控制的是显示*/
background-clip: content-box;
中间搜索大盒子：宽高100%，用padding撑开两侧的距离
搜索input：宽度100%，高度固定

内容部分：
左边：ul和li显示分类名称
右边：头部a链接和h3标题，下面用li表示衣服---li>a>img+p

内容大盒子宽高100%
左边：宽高100%，左侧整体浮动，设overflow：hidden
	ul宽100%，li：宽100%，高固定
	a：宽高100%，设背景色，颜色，边框，鼠标经过时的样式
右侧：右侧大盒子高100%，不设宽，以margin-left撑开距离，
display：flex和flex-direction: column;---垂直显示，一个列
flex-direction:设置弹性盒元素的方向
	右上图片宽100%，图片100%，h3样式
	
详细分类：大盒子宽100%，flex为1，overflow：hidden。li宽33.33%，浮动
flex 属性用于设置或检索弹性盒模型对象的子元素如何分配空间
flex 属性是 flex-grow、flex-shrink 和 flex-basis 属性的简写属性


左侧滑动触摸事件：设置在window.onload内
获取相关参数：
·获取左侧栏的高度
·获取用来滑动的列表的高度--ul:first-of-type
·获取所有的li元素
·设置静止状态下的最大top值--0，最小top值--左侧栏的高度-列表的高度
·设置滑动状态下的最大top值--静止的最大值+100，最小top值--静止最小的-100
实现滑动事件:
·设置起始y坐标，移动的y坐标和之间的距离都为0，记录当前滑动到的距离-起始也为0
·使用touchstart获取起始的y坐标，touchmove获取移动的坐标，并计算距离
--此时判断如果滑动的距离是否超出指定区间，超出提示--当前滑动到的距离加上滑动的而距离大于最大或小于最小
if(currentY+distanceY > maxBounceTop || currentY+distanceY < minBounceTop){
·先将之前的过渡效果清除，然后重新计算偏移的top值=之前基础上加上滑动的距离

回弹效果：
·当触摸事件结束时，通过touchend事件判断--先设置当前的动画效果为top 0.5
·判断当前滑动的距离是否在静止状态和滑动状态下的最小top值之间
如果当前的距离加上滑动的距离<静止状态下的最小值--回到最小值的位置
如果大于静止状态下最大值的距离，回到最大值的距离
如果在两者中间记录当前滑动到的距离（currentY）=当前的距离（currentY）+滑动的距离


鼠标点击li显示当前分类到最顶部：
·通过for循环为每一个li元素设置添加一个索引值
绑定移动端的tap事件
·修改li元素的样式：将所有li元素的active样式清除，再为当前被点击的li元素添加active样式
·移动当前的li元素到父容器的最顶部，但是不能超出之前设定了静止状态下的最小top值
--开启过渡，设置偏移
--如果-（当前索引*当前li的高度）<静止最小值---只能到最小值
否则=ulBox.style.top=-index*liHeight+"px";













